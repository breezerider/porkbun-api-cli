{
  "gates": {
    "lint": "tox -e check",
    "test": "tox -e py313",
    "format": "ruff format {file}",
    "formatPaths": ["src/**/*.py", "tests/**/*.py"],
    "testChanged": [
      { "match": ["src/**/*.py", "tests/**/*.py"], "cmd": "tox -e py313 -- {files}" }
    ]
  }
}
